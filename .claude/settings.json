{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "cmd=$(jq -r '.tool_input.command'); if echo \"$cmd\" | grep -qE 'git commit'; then pnpm lint && pnpm typecheck; fi",
            "timeout": 120,
            "statusMessage": "Verificando lint y tipos..."
          }
        ]
      }
    ]
  }
}
